#include <stdio.h>

int main(){
	int N,K;
	scanf("%d %d", &N, &K);
	char pal[N];

	for(int i = 0; i < N; i++){
		scanf("%s", pal);
}
        for(int i = 0; i < N; i++){
                printf("%s", pal[i]);

}
}
